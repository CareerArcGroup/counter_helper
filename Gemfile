source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# TODO: Update to the new tagged version of redis_helper once it is released with Redis 5.x support
gem 'redis_helper', github: 'CareerArcGroup/redis_helper', tag: '4.0.2'

gemspec
